[alias]
    # Alias để fix submodules
    fixsub = "!./fix-submodules.sh"

    # Alias để pull submodules
    setup = "!./setup.sh"

    
    # Alias để pull tất cả submodules với remote
    pullall = "submodule update --init --recursive --remote"